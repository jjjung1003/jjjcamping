<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybats-3-mapper.dtd">

<mapper namespace="kr.co.jjjcamping.dao.OrderDao">

	<select id="order_first" resultType="kr.co.jjjcamping.dto.CartDto">
		select * from cart where userid=#{param}
	</select>
	
	<insert id="order_second" parameterType="kr.co.jjjcamping.dto.OrderDto">
		insert into pro_order(userid, o_code, o_name, o_phone, zipcode, address, d_address, tot_price, payment, writeday)
		values(#{userid},#{o_code},#{o_name},#{o_phone},#{zipcode},#{address},#{d_address},#{tot_price},#{payment},now())
	</insert>
	
	<select id="order_complete" resultType="kr.co.jjjcamping.dto.OrderDto">
		select * from pro_order where userid=#{param1} and o_code=#{param2}
	</select>
	




</mapper>
